//
//  ThemeColors.swift
//  Breath
//
//  Created by AFP PAR 58 on 21/03/26.
//

import SwiftUI

struct ThemeColors {
    // Backgrounds & Surfaces (Tonal Layering)
    static let surface = Color(hex: "#121416")
    static let surfaceContainerLowest = Color(hex: "#0c0e10")
    static let surfaceContainerLow = Color(hex: "#1a1c1e")
    static let surfaceContainer = Color(hex: "#1e2022")
    static let surfaceContainerHigh = Color(hex: "#282a2c")
    
    // Accents & Actions
    static let primary = Color(hex: "#9fcceb")
    static let primaryContainer = Color(hex: "#2e5d77")
    static let onPrimary = Color(hex: "#00344b")
    
    static let secondary = Color(hex: "#a3cce9")
    static let onSecondary = Color(hex: "#00344c")
    
    static let secondaryContainer = Color(hex: "#214b64")
    static let onSecondaryContainer = Color(hex: "#92bad7")
    
    // The "Golden Hour" Accent
    static let tertiary = Color(hex: "#ffb873")
    
    // Text & Borders
    static let onSurface = Color(hex: "#e2e2e5") // Soft moonlit grey
    static let onSurfaceVariant = Color(hex: "#c2c7cf")
    static let outlineVariant = Color(hex: "#42474e")
    
    static let surfaceTint = Color(hex: "#9fcceb")
}
